dependencies:
    pre:
      - pip install -U pip
      - pip install cython
      - pip install opencv-python
    override:
      - pip install -e .

test:
  pre:
    - pip install coverage
    - pip install tox
